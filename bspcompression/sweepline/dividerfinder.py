import heapq
from cleanslate import area_columns
import numpy as np


def init_sweepline(upper_bounds, lower_bounds):
    columns = area_columns(upper_bounds, lower_bounds)
    pass  # todo


def divider_finder(upper_bounds, lower_bounds, incremental_evaluation):
    lines = np.thing
    state = init_sweepline(upper_bounds, lower_bounds)
    queue = []
    while len(queue) > 0:
        pass


